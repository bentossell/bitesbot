name: droid
command: ~/.local/bin/droid
args:
  - --skip-permissions-unsafe
inputMode: jsonl

resume:
  flag: --session-id
  sessionArg: last

model:
  flag: --model
  default: opus

specMode:
  flag: --use-spec
