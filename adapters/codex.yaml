name: codex
command: codex
args:
  - --json
  - --dangerously-bypass-approvals-and-sandbox
  - --skip-git-repo-check
inputMode: arg

workingDirFlag: --cd

model:
  flag: --model
  default: gpt-5.2-codex
