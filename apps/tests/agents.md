# Agent Instructions

- Define the agent role for this app.
- Describe what the app should do and any constraints.
- Use data.json as optional storage (source of truth if used).
- Update ui.json to reflect the current state (json-renderer).
